import { flavorListQuery } from './queries/flavors';
import { addFlavorMutation } from './mutations/flavors';

const queries = {
  flavorListQuery,
};

const mutations = {
  addFlavorMutation,
};

export {
  queries,
  mutations,
};
